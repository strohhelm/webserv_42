However, if your server needs to support CGI execution, you will likely need to fork() a child process to run the script and use execve() to replace the child process with the CGI script.

handleDelete -> getFullPath






philipp 16.04.25
Simple server is one server or all servers?
should values be pubic or private everywhere? -> setters getters